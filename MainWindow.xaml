<Window x:Class="GetOutAdminV2._0.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:GetOutAdminV2._0"
        xmlns:behaviors="clr-namespace:GetOutAdminV2._0"
        xmlns:converters="clr-namespace:GetOutAdminV2.Converters" 
        xmlns:vm="clr-namespace:GetOutAdminV2.ViewModels"
        Title="GetOut" Height="900" Width="1600"
        Icon="/Resources/Images/logo.png"
        Background="#1E1E1E" 
        WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:NavigationViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="NavBarMarginConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="StyledButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="5"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="10" 
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2A5D9B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <!-- Fond général avec un effet de bord arrondi -->
        <Border Background="#1E1E1E" CornerRadius="20" Padding="10"/>

        <!-- Barre de navigation avec visibilité contrôlée -->
        <Grid VerticalAlignment="Top" Width="250"
      Visibility="{Binding IsNotLogInPage, Converter={StaticResource BooleanToVisibilityConverter}}"
              HorizontalAlignment="Left">
            <Border Background="#6B46C1" CornerRadius="0, 0, 20, 20" Padding="10"/>

            <!-- Utilisation d'un Grid pour un meilleur placement -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <!-- Remplit l'espace restant -->
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Command="{Binding LogInCommand}" Style="{StaticResource StyledButton}">
                    <Label Content="Connexion"/>
                </Button>
                <Button Grid.Row="1" Command="{Binding ListUsersCommand}" Style="{StaticResource StyledButton}">
                    <Label Content="Liste des utilisateurs"/>
                </Button>
                <Button Grid.Row="2" Command="{Binding DashBoardCommand}" Style="{StaticResource StyledButton}">
                    <Label Content="DashBoard"/>
                </Button>
            </Grid>
        </Grid>


        <!-- Contenu principal -->
        <Grid Margin="{Binding IsNotLogInPage, Converter={StaticResource NavBarMarginConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Frame Content="{Binding CurrentPage}" NavigationUIVisibility="Hidden" Background="Black"/>
        </Grid>
    </Grid>
</Window>